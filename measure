#!/bin/sh
# Profile a cold Gradle import in the running IDEA project.
set -eu
measure_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec python3 "$measure_root/tools/gradle-import-profile/profile_import.py" --allow-concurrent "$@"
